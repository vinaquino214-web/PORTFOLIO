{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Portfolio",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://127.0.0.1:5500",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Start Live Server"
    }
  ]
}
